# Recovery scripts

This directory contains incident-specific or historical recovery commands that
are not normal product entry points. Each script must preserve its original
fail-closed behavior and may depend on an archived experiment revision.

Use the maintained commands in the parent `scripts/` directory for current
mapping, verification, benchmarking, and release workflows.
