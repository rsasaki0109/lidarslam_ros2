{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "competitive_execution_machine_identity_pop_input_sidecar_v1",
  "type": "object", "additionalProperties": false,
  "required": ["schema", "schema_version", "artifact_path", "artifact_bytes", "artifact_sha256", "canonical_sha256"],
  "properties": {
    "schema": {"const": "competitive_execution_machine_identity_pop_input_sidecar_v1"},
    "schema_version": {"const": 1},
    "artifact_path": {"type": "string", "minLength": 1, "pattern": "^[A-Za-z0-9._-]+$"},
    "artifact_bytes": {"type": "integer", "minimum": 1, "maximum": 2097152},
    "artifact_sha256": {"$ref": "#/$defs/sha"},
    "canonical_sha256": {"$ref": "#/$defs/sha"}
  },
  "$defs": {"sha": {"type": "string", "pattern": "^[0-9a-f]{64}$"}}
}
